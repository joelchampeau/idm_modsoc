A RoleGui is a GUI to enable user to define roles, define the attachment of roles and see the result.

Instance Variables
	browser:		<GLMTabulator>

browser
	- define the visualisation as a Smalltalk Browser.